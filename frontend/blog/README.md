For most devices, the following command should work:
```
npm start
```


In order to run this application, you need Angular CLI. After Angular CLI is installed, simply execute the following command in your terminal:

```
ng serve
```